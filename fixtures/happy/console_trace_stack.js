// trace — documents actual behavior (may or may not include stack).
// Current implementation may output only the message without full stack.
console.trace("trace-point");
console.log("still-running");
